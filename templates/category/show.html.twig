{% extends 'base.html.twig' %}

{% block title %}{{ category.name }}{% endblock %}

{% block body %}
    <h1>{{ category.name }}</h1>

    <div class="row">
        {{ include('fragments/productsList.hrml.twig', {'products': category.products}) }}
    </div>
{% endblock %}
